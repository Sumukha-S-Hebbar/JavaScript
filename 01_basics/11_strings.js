const stringone = "sumukha"; // String primitive
const stringtwo = 'hebbar'; // String primitive

const string1 = new String("sumukha"); // String object


console.log(`hello ${stringone} ${stringtwo}`);
// Template literal for string interpolation
//by using back ticks we can do string interpolation use this for string concatenation

//learn more about string methods and properties and prototypes

//string methods
"cat".charAt(1);
// gives value at index 1

at(-1)
//same as char at but also takes negative values

endsWith("jumps", 25))
//endswith will take two parameter one is the substring and the other is the length 
//it return true if the string ends with the substring given as paramter

.includes()
//will take a parameter which is substring and return true or false if the substring is present in the string
//case sensitive